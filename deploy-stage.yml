deploy-success:
  extends: .deploy_template
  stage: deploy
  needs:
    - build
  only:
    - master

deploy-fail:
  extends: .deploy_template
  stage: deploy
  script:
    - echo "Deployment failed!"
  needs:
    - build
  only:
    - master
